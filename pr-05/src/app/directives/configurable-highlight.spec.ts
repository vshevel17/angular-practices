import { ConfigurableHighlight } from './configurable-highlight';

describe('ConfigurableHighlight', () => {
  it('should create an instance', () => {
    const directive = new ConfigurableHighlight();
    expect(directive).toBeTruthy();
  });
});
